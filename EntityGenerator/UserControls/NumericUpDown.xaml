<UserControl x:Class="EntityGenerator.UserControls.NumericUpDown"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EntityGenerator.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="16"/>
        </Grid.ColumnDefinitions>
        <RepeatButton Grid.Row="0" Grid.Column="1" IsTabStop="False" Click="RepeatButtonUp_Click" MouseWheel="RepeatButton_MouseWheel">
            <RepeatButton.Content>
                <Viewbox x:Name="ViewBoxUp" Margin="1">
                    <Polygon Points="1,0 2,1 0,1 1,0" Fill="Gray"/>
                </Viewbox>
            </RepeatButton.Content>
        </RepeatButton>
        <RepeatButton Grid.Row="1" Grid.Column="1" IsTabStop="False" Click="RepeatButtonDown_Click" MouseWheel="RepeatButton_MouseWheel">
            <RepeatButton.Content>
                <Viewbox x:Name="ViewBoxDown" Margin="1">
                    <Polygon Points="0,0 2,0 1,1 0,0" Fill="Gray"/>
                </Viewbox>
            </RepeatButton.Content>
        </RepeatButton>
        <TextBox Grid.RowSpan="2" Grid.Column="0"
                 x:Name="TextBox"
                 TextAlignment="Right"
                 VerticalContentAlignment="Center"
                 InputMethod.IsInputMethodSuspended="True"
                 PreviewKeyDown="TextBox_PreviewKeyDown"
                 PreviewTextInput="TextBox_PreviewTextInput"
                 CommandManager.PreviewExecuted="TextBox_PreviewExecuted"
                 GotFocus="TextBox_GotFocus"
                 PreviewMouseLeftButtonDown="TextBox_PreviewMouseLeftButtonDown"
                 MouseWheel="TextBox_MouseWheel"
                 Text="{Binding Path=Value, Mode=TwoWay, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=local:NumericUpDown}}"/>
    </Grid>
</UserControl>
